# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Build
build/
*.o
*.d

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Swift Package Manager
.build/
Packages/
Package.resolved

# CocoaPods (not used but just in case)
Pods/

# Secrets — never commit
# Belt-and-suspenders: covers BOTH the pre-rename (Sunzzari/) and post-rename (Miracles/)
# source folder paths so the rule survives Phase 1.5 Step 8 Xcode project rename.
Sunzzari/Config/Secrets.swift
Miracles/Config/Secrets.swift
